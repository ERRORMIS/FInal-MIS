import styled from "styled-components";

const Wrapper = styled.section`
  .table-topic{
    font-size: 1.2rem;
    font-weight: bold;
    text-align: center;
  } 
  .table {
    margin-top: 20px;
  }
  .delete-icon {
    cursor: pointer;
    font-size: 1.2rem;
  }
`;

export default Wrapper;